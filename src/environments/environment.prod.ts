// Production environment - used by `ng build` (default config = production).
// Swapped in for environment.ts via "fileReplacements" in angular.json.
export const environment = {
    apiUrl: 'https://dimitriskr-pottery-chronology-predictor-api.hf.space',
};
